# Getting started

This is a WordPress theme.

## Docker

```bash
docker compose up
```

Open http://localhost:8080, finish the WordPress install, then activate **AppSeed Theme**.

## Without Docker

Copy this folder into `wp-content/themes/` and activate it.

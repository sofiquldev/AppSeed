# Getting started

This is a WordPress plugin.

```bash
docker compose up
```

Open http://localhost:8080, finish the WordPress install, then activate **AppSeed Plugin**.

Without Docker: copy this folder into `wp-content/plugins/` and activate it.
